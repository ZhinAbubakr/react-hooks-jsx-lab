import React from "react";
import "../index.css";

function NavBar() {
  // update the JSX being returned!
  return (
    <nav>
      <ul>
        <a href="#home">Home</a>
        <a href="#about">About</a>
      </ul>
    </nav>
  );
}

export default NavBar;
